#include<iostream>
using namespace std;
int main()
{
    /*
    int x0,y0,maxn,smallest,sum,i,j,k;
    x0 = y0 = maxn = sum = i = j = k = smallest = 0;
    cin >> x0 >> y0;
    for(i = 1; i <= y0; i++)
    {

        for(j = 1,k = 1;j <= y0,k <= y0;j++,k++)
            {

            if(j % i == 0&&k % i == 0&&i > maxn)
                maxn = i;
            if(j * i == y0&&k * i == y0)
                {
                smallest = j * i;
                smallest = k * i;
                sum = sum + 1;
                }
            if(maxn == x0&&smallest == y0&&sum == 1)
                cout << j << k;
            }

    }
    */
    return 0;
}
